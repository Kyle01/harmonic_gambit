extends Node

## Evaluates the current party composition against the equipped band card
## (Jazz Trio, Power Trio, etc.) and emits EventBus.band_bonus_changed
## with the resulting bonus payload when it changes.


func evaluate(_party: Array[Node], _band_card: Resource) -> Resource:
	return null
