class_name MapNode
extends Resource

## A single node in a region's runtime map graph. Generated by
## RegionPlanner; never authored. position is in viewport coordinates
## of the play scene (the planner picks it). prev_ids enables
## reconvergence checks; next_ids drives the player's available choices.
##
## Uses Resource (not RefCounted) per CLAUDE.md migrability rule:
## "Resource subclasses for records — no duck-typed dicts." Runtime-only
## but we still get inspector/debug visibility for free.

@export var id: int = -1
@export var position: Vector2 = Vector2.ZERO
@export var kind: int = 0
@export var next_ids: Array[int] = []
@export var prev_ids: Array[int] = []
## When non-empty, the (future) event resolver fires this specific EventDef
## on entering this node, bypassing the random event picker. Set by
## RegionPlanner when the owning RegionDef has a starter_event_id.
@export var pinned_event_id: StringName = &""
